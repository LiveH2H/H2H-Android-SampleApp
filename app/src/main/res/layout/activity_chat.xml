<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="itutorgroup.h2h.activity.MainActivity">

    <EditText
        android:hint="Enter text to send"
        android:id="@+id/editText"
        android:layout_width="match_parent"
        android:layout_height="50dp" />

    <Button
        android:id="@+id/sentBtn"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_below="@+id/editText"
        android:onClick="sendBtnClicked"
        android:text="Send"
        android:layout_alignParentRight="true"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:text="Log out"
        android:onClick="logoutBtnClicked"
        android:layout_below="@+id/editText"
        android:id="@+id/logoutBtn"
        android:layout_toLeftOf="@+id/sentBtn"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:text="Log all chats"
        android:onClick="logChatsClicked"
        android:layout_below="@+id/editText"
        android:layout_toLeftOf="@+id/logoutBtn"/>

</RelativeLayout>
